{
  "name": "soham-trading-corp",
  "version": "1.0.0",
  "description": "Soham Trading Corporation Landing Page",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node -e \"const fs = require('fs'); fs.cpSync('images', 'public/images', { recursive: true });\"",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "@types/jest": "^30.0.0",
    "jest": "^30.4.2",
    "netlify-cli": "^26.1.0",
    "vite": "^8.0.16"
  },
  "overrides": {
    "js-yaml": "^4.2.0",
    "esbuild": "0.28.1"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.6"
  }
}
